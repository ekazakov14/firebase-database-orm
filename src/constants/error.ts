const WRONG_PROPERTIES = 'Wrong properties, define all fields by decorator';
const EMPTY_FIELDS_MODEL = 'No one field in model';
const STORAGE_OBJECT_NOT_FOUND = 'storage/object-not-found';

export {
  WRONG_PROPERTIES,
  EMPTY_FIELDS_MODEL,
  STORAGE_OBJECT_NOT_FOUND,
};
